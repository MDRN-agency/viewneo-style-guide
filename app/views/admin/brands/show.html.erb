<p>
  <strong>Name:</strong>
  <%= @brand.name %>
</p>

<p>
  <strong>Summary:</strong>
  <%= @brand.summary %>
</p>

<p>
  <strong>Description:</strong>
  <%= @brand.description %>
</p>

<p>
  <strong>Industry:</strong>
  <%= @brand.industry %>
</p>

<p>
  <strong>Logo:</strong>
  <% if @brand.logo_url %>
  <img src="<%= @brand.logo_url %>" width="100">
  <% else %>
    N/A
  <% end %>
</p>

<p>
  <strong>Thumbnail photo:</strong>
  <% if @brand.thumbnail_photo_url %>
  <img src="<%= @brand.thumbnail_photo_url %>" width="100">
  <% else %>
    N/A
  <% end %>
</p>

<%= link_to 'Edit', edit_admin_brand_path(@brand) %> |
<%= link_to 'Back', admin_brands_path %>
